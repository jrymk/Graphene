/// defines the docking and window layout for the graph view window

#pragma once

#include <imgui.h>
#include <imgui_internal.h>

namespace graphene {
    namespace editor {
        namespace graphview {
            void windowBegin() {


            }

        }
    }
}