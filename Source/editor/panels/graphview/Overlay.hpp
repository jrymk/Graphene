#pragma once

#include "../../../../Dependencies/imgui/imgui.h"

#include "Common.hpp"

namespace graphene::editor::graphview::overlay {

void draw() {
	/*
		ImGui::GetWindowDrawList()->AddText(ImVec2(view::canvasCursor.x + 5.0f, view::canvasCursor.y + 5.0f),
											ImGui::ColorConvertFloat4ToU32(ImVec4(0.59, 0.80, 0.81, 1.0f)), "Framerate",
	   0); ImGui::GetWindowDrawList()->AddText(ImVec2(view::canvasCursor.x + 5.0f, view::canvasCursor.y + 15.0f),
											ImGui::ColorConvertFloat4ToU32(ImVec4(0.59, 0.80, 0.81, 1.0f)),
											std::to_string(view::framerateCounter.getFramerate()).c_str(), 0);

		ImGui::GetWindowDrawList()->AddText(ImVec2(view::canvasCursor.x + 5.0f, view::canvasCursor.y + 40.0f),
											ImGui::ColorConvertFloat4ToU32(ImVec4(0.81, 0.72, 0.59, 1.0f)),
											"Graph update rate", 0);
		ImGui::GetWindowDrawList()->AddText(ImVec2(view::canvasCursor.x + 5.0f, view::canvasCursor.y + 50.0f),
											ImGui::ColorConvertFloat4ToU32(ImVec4(0.81, 0.72, 0.59, 1.0f)),
											std::to_string(Graphene::core->updateRateCounter.getFramerate()).c_str(),
	   0);*/
}

} // namespace graphene::editor::graphview::overlay
